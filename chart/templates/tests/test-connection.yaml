apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hiiirepdf.name" . }}-test-connection"
  labels:
    {{- include "hiiirepdf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-qO-', 'http://{{ include "hiiirepdf.name" . }}:{{ .Values.service.port }}/']
  restartPolicy: Never
